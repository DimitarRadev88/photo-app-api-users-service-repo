spring:
  application:
    name: users-ws

  config:
    import: optional:configserver:http://localhost:8888

  cloud:
    config:
      uri: https://localhost:8888
      username: client
      password: 123456